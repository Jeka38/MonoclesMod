<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/color_background_secondary">

    <eu.siacs.conversations.ui.widget.TextureViewRenderer
        android:id="@+id/participant_video"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <eu.siacs.conversations.ui.widget.SpeakingBorderView
        android:id="@+id/participant_speaking"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/participant_avatar"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:shapeAppearance="@style/ShapeAppearanceOverlay.PhotoRound" />

    <TextView
        android:id="@+id/participant_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#88000000"
        android:ellipsize="end"
        android:maxLines="1"
        android:padding="6dp"
        android:textColor="@android:color/white"
        android:textSize="13sp" />
</FrameLayout>
